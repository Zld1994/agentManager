#
# Development compose file.
# The defaults below are for local use only and are not production-safe.
#
services:
  agentmanager:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: agentmanager-api
    env_file:
      - .env.example
    environment:
      API_HOST: 0.0.0.0
      API_PORT: 8000
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER:-agentmanager}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-agentmanager}
      POSTGRES_DB: ${POSTGRES_DB:-agentmanager}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      QDRANT_HOST: qdrant
      QDRANT_PORT: 6333
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin}
      MINIO_BUCKET: ${MINIO_BUCKET:-agentmanager}
      OTEL_TRACING_ENABLED: ${OTEL_TRACING_ENABLED:-false}
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4317}
      AUDIT_SINK: ${AUDIT_SINK:-log}
      AUDIT_REDACT_FIELDS: ${AUDIT_REDACT_FIELDS:-api_key,password,token}
      DEBUG: ${DEBUG:-True}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      qdrant:
        condition: service_healthy
      minio:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3).read()",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - agentmanager

  otel-collector:
    image: docker.m.daocloud.io/otel/opentelemetry-collector-contrib:0.102.1
    container_name: agentmanager-otel-collector
    command: ["--config=/etc/otel-collector-config.yml"]
    volumes:
      - ./monitoring/otel-collector-config.yml:/etc/otel-collector-config.yml:ro
    ports:
      - "4317:4317"
      - "4318:4318"
    depends_on:
      - jaeger
    networks:
      - agentmanager

  jaeger:
    image: docker.m.daocloud.io/jaegertracing/all-in-one:1.57
    container_name: agentmanager-jaeger
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
    ports:
      - "16686:16686"
    networks:
      - agentmanager

  postgres:
    image: docker.m.daocloud.io/library/postgres:15
    container_name: agentmanager-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-agentmanager}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-agentmanager}
      POSTGRES_DB: ${POSTGRES_DB:-agentmanager}
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - agentmanager

  redis:
    image: docker.m.daocloud.io/library/redis:7
    container_name: agentmanager-redis
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    networks:
      - agentmanager

  qdrant:
    image: docker.m.daocloud.io/qdrant/qdrant:latest
    container_name: agentmanager-qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant-data:/qdrant/storage
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "bash -ec ':> /dev/tcp/127.0.0.1/6333'",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - agentmanager

  minio:
    image: docker.m.daocloud.io/minio/minio:latest
    container_name: agentmanager-minio
    command: ["server", "/data", "--console-address", ":9001"]
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-minioadmin}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - agentmanager

volumes:
  postgres-data:
  redis-data:
  qdrant-data:
  minio-data:

networks:
  agentmanager:
    driver: bridge
