# Project Map

**Project:** agentManager

## Modules

- **agentManager.agents** (`agentManager\agents`)
- **agentManager.config** (`agentManager\config`)
- **agentManager.defect_repair** (`agentManager\defect_repair`)
- **agentManager.domain** (`agentManager\domain`)
- **agentManager.engine** (`agentManager\engine`)
- **agentManager.engine.event_bus** (`agentManager\engine\event_bus`)
- **agentManager.memory** (`agentManager\memory`)
- **agentManager.observability** (`agentManager\observability`)
- **agentManager.recovery** (`agentManager\recovery`)
- **agentManager.roles** (`agentManager\roles`)
- **agentManager.runtime** (`agentManager\runtime`)
- **agentManager.sandbox** (`agentManager\sandbox`)
- **agentManager.scheduler** (`agentManager\scheduler`)
- **agentManager.storage** (`agentManager\storage`)
- **scheduler** (`scheduler`)
- **tests.benchmarks** (`tests\benchmarks`)
- **tests.integration** (`tests\integration`)
- **tests.unit.memory** (`tests\unit\memory`)

## Entry Points

- `agentManager/api.py`
