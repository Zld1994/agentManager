[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agentManager"
version = "0.1.0"
description = "AI Agent Orchestration Control Plane Prototype"
readme = "README.md"
requires-python = ">=3.10"
authors = [
    {name = "Zld1994", email = "649077883@qq.com"}
]
license = {text = "MIT"}

dependencies = [
    "pydantic>=2.0",
    "python-dotenv>=1.0",
    "networkx>=3.0",
    "fastapi>=0.104.0",
    "uvicorn>=0.24.0",
    "redis>=5.0.0",
    "prometheus-client>=0.17.0",
    "psutil>=5.9",
]

[project.optional-dependencies]
dev = [
    "build>=1.0",
    "black>=23.0",
    "isort>=5.12",
    "flake8>=6.0",
    "mypy>=1.0",
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "pytest-cov>=4.0",
    "pytest-mock>=3.10",
    "httpx>=0.25",
]

sandbox = [
    "docker>=6.1",
]

[tool.setuptools]
packages = {find = {where = ["."], include = ["agentManager", "agentManager.*", "scheduler", "scheduler.*"]}}

[tool.black]
line-length = 100
target-version = ["py310"]

[tool.isort]
profile = "black"
line_length = 100

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --cov=agentManager --cov-report=term-missing"
asyncio_mode = "auto"
